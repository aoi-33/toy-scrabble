# Scrabble 実装レビュー

レビュー日: 2026-09-06

ゲーム進行に影響する不具合を確認しました。既存テスト91件・lint・build はすべて成功していますが、追加の一時テスト10件で以下を再現しました。レビュー時点で実装ファイルの変更はありません。一時テストは確認後に削除しています。

## 指摘事項

### 1. 【高】COM の結果が人間の手番にも適用される

Worker が500ms以内に応答すると、表示待ちの間に同じ手番の探索を再要求します。遅れた結果に手番確認がなく、人間の手番が勝手に PASS されることを、Worker を模擬した画面テストで再現しました。

要求中の管理と、結果適用時の手番照合が必要です。

該当: [src/App.tsx](src/App.tsx) 47行目、[src/ai/useAiWorker.ts](src/ai/useAiWorker.ts) 25行目

### 2. 【高】手札と袋が空になっても終了しない

`COMMIT_PLAY` と `COMMIT_AI_PLAY` が終了判定を呼んでいません。最後のタイルを使っても `playing` のままで、残タイルによる得点精算も行われません。

該当: [src/game/reducer.ts](src/game/reducer.ts) 166行目・220行目

### 3. 【高】NEW GAME が盤面と袋を初期化しない

前ゲームの盤面を残し、残った袋から再配布します。6回 PASS 後の再開では袋が86枚から72枚に減り、袋が空なら手札も配られません。

該当: [src/game/reducer.ts](src/game/reducer.ts) 72行目

### 4. 【高】仮配置中の PASS／EXCHANGE でタイルの所属・枚数が壊れる

PASS は仮配置を次プレイヤーに引き継ぐため、相手が RECALL すると相手の手札に入ります。EXCHANGE は仮配置を手札へ戻さず削除し、タイル総数が100枚から99枚になる例を再現しました。

該当: [src/game/reducer.ts](src/game/reducer.ts) 262行目・272行目

### 5. 【高】COM が単語の途中を合法手として登録する

単語の直後に既存タイルが続いていても候補に登録します。テスト辞書で `CAT` を許可、`CATS` を不許可とした場合、実際には `CATS` になる配置を `CAT` として生成しました。確定時に拒否され、手番が進まない原因になります。

該当: [src/ai/search.ts](src/ai/search.ts) 196行目・229行目

### 6. 【中】初手は任意の1文字だけでも確定できる

中央に `Q` を1枚置くと、辞書にないにもかかわらず0点で成功します。形成単語が空の場合に、辞書チェックを通過してしまいます。

該当: [src/game/rules.ts](src/game/rules.ts) 76行目

### 7. 【中】RECALL したブランクの文字を変更できない

指定済みの文字が手札へ戻っても保持され、再配置時に文字選択画面が開きません。確定前の配置をやり直す際に、別の文字を指定できなくなります。

該当: [src/game/reducer.ts](src/game/reducer.ts) 108行目、[src/App.tsx](src/App.tsx) 148行目

### 8. 【中】COM が左・上へ延ばす合法手を見落とす

アンカーより前の空マスへ手札を置く探索がありません。既存の `T` の左に `CA` を置ける条件でも、候補0件になる例を確認しました。不要な交換・PASSにつながります。

該当: [src/ai/search.ts](src/ai/search.ts) 43行目

### 9. 【中】COM の評価点で既存ブランクが通常得点になる

交差単語の計算で小文字を大文字化し、ブランクの0点という情報を失います。`Z` 指定のブランクを含む例では、実際より20点高く評価しました。確定得点ではなく、COM の手の選択に影響します。

該当: [src/ai/search.ts](src/ai/search.ts) 140行目

## 検証結果とテストの不足

- `npm test`: 15ファイル、91件成功。
- `npm run lint`: 成功。
- `npm run build`: 成功。
- 追加の一時テスト: 10件で上記の不具合を再現。COM の非同期処理は模擬 Worker とタイマーを用いて検証。

既存テストは個別処理の正常系が中心です。特に COM の非同期連携、配置確定による終局、再ゲーム、仮配置を残した手番終了の回帰テスト追加が必要です。

記載した行番号はレビュー時点のものです。
